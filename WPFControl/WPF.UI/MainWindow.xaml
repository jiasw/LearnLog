<Window x:Class="WPF.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ed="http://schemas.microsoft.com/expression/2010/drawing"
        xmlns:ec="http://schemas.microsoft.com/expression/2010/controls"
        xmlns:local="clr-namespace:WPF.UI"
        xmlns:ctl="clr-namespace:WPF.UI.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<ctl:CircularProgressBar x:Name="circleProgressBar" CurrentValue="0" />-->

        <StackPanel Orientation="Vertical">
           
                <ctl:PathControl Height="200" Maximum="180"  x:Name="pathControl"   />
            

        </StackPanel>
        
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Name="btnStart" Content="开始" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click" />
            <TextBox Name="txtangle" LostFocus="txtangle_LostFocus" Background="Gray"></TextBox>
            <TextBox Name="txtresult"></TextBox>
        </StackPanel>
    

        
    </Grid>
</Window>
